const AdminServices = () => (
  <div>
    <h1 className="font-display font-bold text-2xl mb-6">Services Manager</h1>
    <p className="text-muted-foreground">Services are currently managed through the codebase. Connect Lovable Cloud to enable dynamic service management.</p>
  </div>
);

export default AdminServices;
